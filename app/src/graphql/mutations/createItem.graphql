mutation CreateItem(
  $name: String!
  $category: ID!
  $brand: ID!
  $model: String!
) {
  createItem(
    input: {
      name: $name
      model: $model
      brandId: $brand
      categoryId: $category
    }
  ) {
    id
    code
  }
}
